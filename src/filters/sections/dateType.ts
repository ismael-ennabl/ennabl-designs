import type { Section } from '../types'

export const dateType: Section = {
  id: 'dateType',
  label: 'Date & Type',
  fields: [],
  childSectionIds: ['premiumType', 'productionAllocation', 'policyStatus', 'dateRange'],
}
